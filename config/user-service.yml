server:
  port: 8081

spring:
  config:
    import: "classpath:application-eureka.yml"
  application:
    name: user-service
  cloud:
    vault:
      kv:
        enabled: true
        backend-version: 2
      fail-fast: false
      authentication: TOKEN
  datasource:
    url: ${database.url}
    driver-class-name: org.h2.Driver
    username: ${database.username}
    password: ${database.password}
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        web-allow-others: true

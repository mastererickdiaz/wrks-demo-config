server:
  port: 8082

spring:
  application:
    name: order-service
  cloud:
    vault:
      uri: http://vault:8200
      token: myroot
      kv:
        enabled: true
        backend-version: 2
      fail-fast: false
      authentication: TOKEN